import './Home.scss';

function Home () {
    return(
        <div className='home'>
            This is where students choose their major (category)
            and from here they are being redirected to the list of jobs in
            that category
        </div>
    );
}

export default Home;
